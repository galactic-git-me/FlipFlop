export const metadata = {
  title: 'PC Cases 3D Review Gallery',
  description: 'View 3D models and reference materials for PC cases',
};

export default function Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
