version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pc-flipper-web
    restart: always
    ports:
      - "${FRONTEND_PORT}:${FRONTEND_CONTAINER_PORT}"
    environment:
      - NODE_ENV=production
